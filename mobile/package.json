{
  "name": "stock-observer-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "台股多維度全方位觀測儀 — Android APK",
  "scripts": {
    "sync": "node scripts/sync-web.mjs",
    "cap:sync": "npm run sync && npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
